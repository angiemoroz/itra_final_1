<h1>Editing User Instruction</h1>

<%= render 'form', user_instruction: @user_instruction %>

<table class="table table-hover">
<tr><td>
Instruction Steps
</td></tr>
<tr> <td><% @steps.each do |instruction_step| %>
        
          <%= link_to instruction_step.title, instruction_step, class: "btn btn-default" %>
                    
                 
      
 <% end %></td></tr>
 <tr><td>
 <%= link_to 'Add step', new_instruction_step_path(user_instruction_id: @user_instruction.id), class: "btn btn-default add" %>
 </td></tr>
 </table>

<%= link_to 'Back', @user_instruction.user, class: "btn btn-default" %>



<style rel='stylesheet'>
.table{
    margin: auto;
    text-align: center;
    font-size: 20px;
  }

</style>